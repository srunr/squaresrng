project Squaresrng is

   for Source_Dirs use ("src");
   for Object_Dir use "obj";
   for Main use ("Example.adb");

   package Compiler is
      for Switches ("ada") use ("-O3", "-gnatwa");
   end Compiler;

   package Ide is
      for Vcs_Repository_Root use ".";
   end Ide;

end Squaresrng;

